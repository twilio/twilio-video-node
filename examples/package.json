{
  "name": "twilio-video-node-examples",
  "private": true,
  "description": "Dependencies for the examples. Every example mints an Access Token through helpers/token.js, which needs the `twilio` helper library; the computer-vision examples additionally need onnxruntime-node and @napi-rs/canvas. Install with `npm install` from this directory.",
  "dependencies": {
    "@napi-rs/canvas": "^1.0.8",
    "onnxruntime-node": "1.22.0",
    "twilio": "^5.12.1"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
